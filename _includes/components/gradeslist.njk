<ul>
  {%- for entry in gradelist %}
    {% if entry.url %}
      <li style="order: {{ entry.data.order }}">
        <a href="{{ entry.url | url }}" {% if entry.url == page.url %} data-current="current item"{% endif %}>{{ entry.data.title }}</a>
      </li>
    {% endif %}
  {%- endfor %}
</ul>